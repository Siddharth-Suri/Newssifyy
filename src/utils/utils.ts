export const lower = 0
export const upper = 10
export const pageSize = 10
export const handlePagination = (props: number[]) => {
    const totalProducts = props.length
    const noOfPages = totalProducts / pageSize
}
